interface:
  display_name: "Figma Context Extractor"
  short_description: "Fetch raw Figma JSON with asset and optional render URLs"
  default_prompt: "Use $figma-context-extractor to fetch raw Figma JSON from a URL or node IDs, include fill asset URLs by default, and add render URLs only when needed."
