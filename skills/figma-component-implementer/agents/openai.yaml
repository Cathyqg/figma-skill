interface:
  display_name: "Figma Component Implementer"
  short_description: "Implement React components from spec/figma raw context"
  default_prompt: "Use $figma-component-implementer to read Figma raw context from spec/figma and map it into reuse-first React component and Storybook changes that follow repository rules."
